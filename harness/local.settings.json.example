# Function App env. Local dev only — Function App runtime gets values from
# Azure App Settings populated by `infrastructure/main.bicep`.
#
# Run `func start` against this when iterating locally.

{
  "IsEncrypted": false,
  "Values": {
    "FUNCTIONS_WORKER_RUNTIME": "python",
    "AzureWebJobsFeatureFlags": "EnableWorkerIndexing",
    "AzureWebJobsStorage__accountName": "REPLACE_WITH_STORAGE_ACCOUNT_NAME",
    "APPLICATIONINSIGHTS_CONNECTION_STRING": "",
    "AZURE_CLIENT_ID": "",
    "AZURE_KEYVAULT_NAME": "",
    "AZURE_STORAGE_ACCOUNT": "",
    "AZURE_STORAGE_BRIEFING_CONTAINER": "briefing-context",
    "AZURE_STORAGE_CAPTURE_QUEUE": "capture-events",
    "AZURE_AI_PROJECT_ENDPOINT": "",
    "AZURE_AI_AGENT_NAME": "companion",
    "TELEGRAM_BOT_TOKEN": "",
    "TELEGRAM_WEBHOOK_SECRET": "",
    "TELEGRAM_ALLOWED_CHAT_ID": "",
    "BRIEFING_ENCRYPTION_KEY": "",

    "// --- dig (deep research Mode B)": "PAT with issues:write on mindVault; in prod use a Key Vault reference",
    "DIG_GITHUB_TOKEN": "",
    "DIG_REPO": "samoletovs/mindVault",

    "// --- reaper poller (github_reapers.py)": "PAT with Actions:RW + Pull requests:R + Contents:R on mindVault AND familyVault; falls back to DIG_GITHUB_TOKEN (mindVault only) if unset",
    "REAPER_GITHUB_TOKEN": "",

    "// --- Tracing (agentFlow Phase 1)": "Hard Rule 9 — never capture content",
    "OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT": "false",
    "OTEL_PYTHON_DISABLED_INSTRUMENTATIONS": "httpx,requests,urllib,urllib3,aiohttp-client",
    "// OTEL_EXPORTER_OTLP_ENDPOINT": "Set to http://localhost:4318 to also export to AI Toolkit local viewer"
  }
}

# Function App runtime deps. Mirrors top-level requirements.txt where they
# overlap; pinned to versions known to work on Flex Consumption Python 3.11.

azure-functions==1.21.3
azure-ai-projects==2.1.0
azure-identity==1.25.3
azure-storage-blob==12.28.0
azure-storage-queue==12.13.0
openai==2.36.0
httpx==0.28.1

# Phase 2 tracing (2026-05-17, agentFlow Phase 1) — Application Insights via
# OpenTelemetry. Reads APPLICATIONINSIGHTS_CONNECTION_STRING from app settings.
# We deliberately do NOT install opentelemetry-instrumentation-httpx — auto-
# instrumented httpx spans capture full URLs, and Telegram URLs contain the
# bot token. Manual spans only for HTTP. See AGENTS.md Hard Rule 9.
azure-monitor-opentelemetry==1.8.8

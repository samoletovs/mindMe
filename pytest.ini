[pytest]
# The harness modules are imported by name (``import function_app``); the
# rootdir-relative path is added by harness/tests/conftest.py.
testpaths = harness/tests
python_files = test_*.py
addopts = -ra
asyncio_mode = strict
